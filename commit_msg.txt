feat(api): Implement user profile update and password change

Adds the PATCH /users/me endpoint allowing authenticated technicians to update their profiles.
Implements UpdateUserUseCase with email collision validation and conditional password hashing using passlib.
Adds comprehensive TDD test coverage for profile edits, identity collision, and security boundaries.
